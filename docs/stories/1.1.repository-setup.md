# Story 1.1: Repository Setup

## Status
Approved

## Story
**As a** developer,
**I want** to establish a complete monorepo project structure with proper configuration and documentation,
**so that** the team can begin development with a clear, organized, and properly configured foundation.

## Acceptance Criteria
1. Monorepo structure created with apps/web, apps/api, and packages/shared directories
2. Package.json workspaces configured with proper dependency management
3. Git repository initialized with appropriate .gitignore and pre-commit hooks
4. Comprehensive README.md created with setup instructions and development workflow
5. Environment configuration files created with validation schemas
6. Development scripts configured for common tasks (dev, build, test, lint)
7. Initial commit made with all foundational configuration files

## Tasks / Subtasks
- [ ] Create monorepo directory structure (AC: 1)
  - [ ] Set up root package.json with workspace configuration
  - [ ] Create apps/web/frontend package.json
  - [ ] Create apps/api/backend package.json
  - [ ] Create packages/shared package.json
- [ ] Configure shared dependencies and workspace management (AC: 2)
  - [ ] Install and configure TypeScript as shared dependency
  - [ ] Set up shared type definitions in packages/shared
  - [ ] Configure workspace scripts and cross-package references
- [ ] Initialize Git repository with proper configuration (AC: 3)
  - [ ] Create comprehensive .gitignore for monorepo
  - [ ] Set up husky for Git hooks
  - [ ] Configure lint-staged for pre-commit validation
  - [ ] Create initial commit with foundational structure
- [ ] Create comprehensive project documentation (AC: 4)
  - [ ] Write README.md with project overview and goals
  - [ ] Document development setup and environment requirements
  - [ ] Create development workflow and contribution guidelines
  - [ ] Document monorepo structure and package relationships
- [ ] Set up environment configuration management (AC: 5)
  - [ ] Create .env.example files for all packages
  - [ ] Set up environment variable validation with zod
  - [ ] Configure environment-specific settings
- [ ] Configure development tooling and scripts (AC: 6)
  - [ ] Set up root-level scripts (dev, build, test, lint)
  - [ ] Configure package-specific development scripts
  - [ ] Set up parallel development for frontend and backend

## Dev Notes

### Project Structure
```
gmaoapp/
├── apps/
│   ├── web/                 # Frontend application (React + Vite)
│   │   ├── src/
│   │   ├── public/
│   │   ├── package.json
│   │   └── vite.config.ts
│   └── api/                 # Backend application (Express + TypeScript)
│       ├── src/
│       ├── package.json
│       └── tsconfig.json
├── packages/
│   ├── shared/              # Shared types and utilities
│   │   ├── src/
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── ui/                  # Shared UI components
│   │   ├── src/
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── config/              # Shared configuration (ESLint, TypeScript)
│       ├── eslint/
│       ├── typescript/
│       └── jest/
├── infrastructure/
│   └── supabase/
├── scripts/
├── docs/
├── .env.example
├── package.json
├── pnpm-workspace.yaml
└── README.md
```

### Key Technologies from Architecture
- **Package Manager:** pnpm with workspace support
- **Monorepo Tool:** npm workspaces (simplicity for 2-4 week timeline)
- **Version Control:** Git with husky hooks
- **Language:** TypeScript 5.0+ across all packages

### Development Scripts
```json
{
  "scripts": {
    "dev": "pnpm --parallel --filter web dev --filter api dev",
    "build": "pnpm --filter web build --filter api build",
    "test": "pnpm --recursive test",
    "lint": "pnpm --recursive lint",
    "type-check": "pnpm --recursive type-check"
  }
}
```

### Environment Configuration
- Frontend: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
- Backend: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, JWT_SECRET
- Shared: Database connection strings, API endpoints

### Testing
- Test framework: Vitest for unit/integration tests
- Test location: Each package includes __tests__/ directory
- Test standards: >80% code coverage for business logic
- Testing patterns: AAA (Arrange, Act, Assert) structure

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-11-07 | v1.0 | Initial story creation | Sarah (PO) |

## Dev Agent Record
*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References
*To be filled by dev agent*

### Completion Notes List
*To be filled by dev agent*

### File List
*To be filled by dev agent*

## QA Results
*Results from QA Agent review will be populated here*